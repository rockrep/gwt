#!/usr/bin/env ruby

require 'sinatra'
require 'gwt/toolkit'

# otherwise sinatra won't always automagically launch its embedded 
# http server when this script is executed
set :run, true

get '/' do
  time_keeper = GWT::Toolkit.new
  return toolkit.awaken
end
